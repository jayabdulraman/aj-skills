interface:
  display_name: "To PRD"
  short_description: "Turn context into a tracker-ready PRD"
  default_prompt: "Use $to-prd to turn our current context into a PRD."
policy:
  allow_implicit_invocation: true
